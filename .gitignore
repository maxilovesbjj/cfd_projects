# --- Python & System Junk ---
__pycache__/
*.py[cod]
.DS_Store
.vscode/

# --- OpenFOAM Generated Files ---

# 1. Logs and temporary files
*.log
log.*
logs/

# 2. Time directories (Numerical directories generated by solver)
# We ignore directories starting with a number, but we MUST keep '0' (initial conditions)
# The pattern below ignores 1, 10, 0.1, etc., but keeps 0
base_case/[1-9]*
base_case/0.*

# 3. Parallel processing directories
base_case/processor*

# 4. Post-processing generated data
base_case/postProcessing/
base_case/VTK/

# 5. Mesh files (Generated by blockMesh/snappyHexMesh)
# We keep the dictionaries in /system, but ignore the binary mesh
base_case/constant/polyMesh/
!base_case/constant/polyMesh/blockMeshDict

# 6. Dynamic Code
base_case/dynamicCode/